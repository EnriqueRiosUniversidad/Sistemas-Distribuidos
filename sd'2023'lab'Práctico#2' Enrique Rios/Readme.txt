Bueno.
        ______Este programa sigue las siguientes instrucciones______
1. Crea una carpeta llamada "ArchivosApp" en tu sistema.
2. Dentro de la carpeta "ArchivosApp", crea una subcarpeta llamada "src".
3. Crea las siguientes clases dentro de la carpeta "src":
        Main.java: Clase principal que demuestra el uso de Apache Commons IO.
        Archivo.java: Clase que utiliza funciones de Apache Commons IO para manipulación de
            archivos.
4. Crea un archivo llamado "build.xml" en la carpeta "ArchivosApp".
5. Dentro del archivo "build.xml", crea una tarea de construcción que realice las
siguientes acciones (todas utilizando properties):
6. Compile las clases en la carpeta "src" y colóquelas en una carpeta de salida llamada
"build".
7. Empaquete el proyecto en un archivo JAR.
8. Crear la tarea run que ejecute el main class.

        ______Ejecucion______
Se debe abrir un CMD en la carpeta ArchivosApp y ejecutar dentro del CMD el comando ANT
Por favor ejecutar el programa para poder tener el JAR y el TextFile.txt, asi
como los archivos compilador dentro de carpeta build que que se crea al ejecutar 


        ______Obstaculos______
Fue arduo encontrar, configurar y resolver problemas de la libreria Apache Commons IO.
Tambien lo fue el no haber tocado codigo java en al menos 2 a casi 3 años
Una vez creado un programa funcional en Java, mis dificultades fueron palear el aprender XML desde 0,
y las etiquetas de ANT. 
Despues los multiples bugs
Hacer todo esto me tomo de las 7 am a las 5pm, alrededor de 10 horas




